# StealthQL code-only data
This directory holds the StealthQL code-WAF audit ledger for this
project. The MCP appends one JSON line per tool call to events.ndjson
so you can replay any agent session forensically.

Add this directory to your .gitignore.
